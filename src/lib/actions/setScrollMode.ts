import { ActionDispatcher } from '../../model/actions/actionDispatcher';
import { SetScrollMode } from '../../model/actions/global';

const setScrollMode: ActionDispatcher<SetScrollMode> = async () => (
   {
   }
);


export default setScrollMode;
